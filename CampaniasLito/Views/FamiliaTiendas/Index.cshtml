@model IEnumerable<CampaniasLito.Models.FamiliaTienda>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Familia.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tienda.Clasificacion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Familia.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tienda.Clasificacion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FamiliaTiendaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.FamiliaTiendaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FamiliaTiendaId })
        </td>
    </tr>
}

</table>
