@model IList<CampaniasLito.Models.TiendaArticulo>

@{
    ViewBag.Title = "Asignar Materiales";
}

@using (Html.BeginForm("AsignarArticulos", "Restaurantes", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()
    <div class="panel-heading">
        <table class="headingModal">
            <tr style="background-color: #DF2530 !important">
                <td class="col-xs-12">
                    <h4 class="responsivoUsuarioLabelFa" style="color: #fff; text-align: center">@ViewBag.Tienda</h4>
                </td>
                <td class=" col-xs-12"></td>
                <td align="right" class="col-xs-1">
                    <div class="input-group">
                        <button type="button" class="botonF2 btn-danger" style="background-color: red" data-dismiss="modal" onclick="viewPageSwal()">
                            <i class="fas fa-times-circle"></i>
                        </button>
                    </div>
                </td>
                <td align="center" class="col-xs-1">
                    <div class="input-group">
                        <button type="submit" class="botonF1Modal btn-success" style="background-color: green" onclick="viewPage2()">
                            <i class="fas fa-check-circle"></i>
                        </button>
                    </div>
                </td>
            </tr>
            <tr>
                <td colspan="4" align="center" style="text-align: center">
                    @Html.DisplayNameFor(model => model.FirstOrDefault().ArticuloKFC.Descripcion)
                </td>
            </tr>

        </table>
    </div>

    <div class="panel-body">
        <div id="unId40" class="table-responsive">


            <form id="form">
                @foreach (var item in Model)
                {
                    <fieldset id="SubmitForm">
                        @*@Html.HiddenFor(m => item.TiendaArticuloId, new { @id = "TiendaArticuloId" })*@
                        @Html.HiddenFor(m => item.ArticuloKFC)
                        @Html.HiddenFor(m => item.ArticuloKFCId)
                        @Html.HiddenFor(m => item.Tienda)
                        @Html.HiddenFor(m => item.TiendaId)
                        <input type="hidden" name="TiendaArticuloId" value="@item.TiendaArticuloId" />
                        <hr />
                        <div class="form-group">
                            <div class="col-sm-10">
                                @Html.DisplayFor(modelItem => item.ArticuloKFC.Descripcion, new { htmlAttributes = new { @class = "form-control responsivoUsuarioText inputModal", @style = "text-transform:uppercase" } })
                            </div>
                            <div class="col-sm-2">
                                <input class="form-control form-check checkbox" id="Seleccionado" name="Seleccionado" type="checkbox" value="@item.TiendaArticuloId" checked="@item.Seleccionado" style="cursor: pointer" />
                            </div>
                        </div>
                        <hr />
                    </fieldset>
                }
            </form>



            @*<table class="table table-striped table-hover" style="width:100%" id="dataTable" cellspacing="0">
                    <tbody>
                        @foreach (var item2 in Model)
                        {
                        <tr>
                            <td hidden="hidden">
                                <input type="hidden" name="articuloId" value="@item2.TiendaArticuloId" />
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item2.ArticuloKFC.Descripcion, new { htmlAttributes = new { @class = "form-control responsivoUsuarioText inputModal", @style = "text-transform:uppercase" } })
                            </td>
                            <td align="center">
                                <input class="form-control form-check checkbox" type="checkbox" value="@item2.TiendaArticuloId" name="Seleccionado" data-id="@item2.TiendaArticuloId" id="EditarArticuloTienda" onclick="seleccionado(this)" checked="@item2.Seleccionado" style="cursor:pointer" />
                            </td>
                        </tr>
                        }
                    </tbody>
                </table>*@

            @*<div class="form-group">
                    @foreach (var item in Model)
                    {
                        <div class="col-sm-12">
                            @Html.EditorFor(modelItem => item.Tienda.Restaurante, new { htmlAttributes = new { @class = "form-control inputModal", @disabled = "disabled", @style = "text-transform:uppercase", placeholder = "Material" } })
                        </div>
                    }
                </div>
                <div class="form-group">
                    @foreach (var item in Model)
                    {
                        <div class="col-sm-10">
                            @Html.EditorFor(modelItem => item.ArticuloKFC.Descripcion, new { htmlAttributes = new { @class = "form-control inputModal", @disabled = "disabled", @style = "text-transform:uppercase", placeholder = "Material" } })
                        </div>
                        <div class="col-sm-2" style="text-align: center">
                            @Html.CheckBoxFor(modelItem => item.Seleccionado, htmlAttributes: new { @class = "form-check" })
                        </div>
                    }
                </div>*@
        </div>
        <br />
        <br />
        <br />
        <br />
    </div>
}
@*<script src="~/Scripts/jquery-3.4.1.min.js"></script>
    <script src="~/Scripts/bootstrap.min.js"></script>*@
@*<link href="~/Content/bootstrap.min.css" rel="stylesheet" />*@

@*<script>

        $("#EditarArticuloTienda").click(function () {
            var data = $("#SubmitForm").serialize();
            var id = $("#TiendaArticuloId").val();
            var sel = false;

            if (document.getElementById("EditarArticuloTienda").checked) {
                sel = true;
            }
            else {
                sel = false;
            }

            $.ajax({
                type: "Post",
                url: "/Tiendas/Editar?id=" + id + "&sel=" + sel,
                data: data,
                success: function (result) {
                    //alert("Success!..");
                    //window.location.href = "/Home/index";
                    //$("#MyModal").modal("hide");

                }
            })
        })

    </script>*@

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
