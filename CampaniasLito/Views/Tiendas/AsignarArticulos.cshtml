@model IList<CampaniasLito.Models.TiendaArticulo>

@{
    ViewBag.Title = "Asignar Materiales";
    Layout = null;
}

@using (Html.BeginForm("Edit", "Tiendas", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()
        <div class="panel-heading">
            <table class="headingModal">
                <tr style="background-color: #DF2530 !important">
                    <td class="col-xs-12">
                        <h4 class="responsivoUsuarioLabelFa" style="color: #fff; text-align: center">@ViewBag.Tienda</h4>
                    </td>
                    <td class=" col-xs-12"></td>
                    <td align="right" class="col-xs-1">
                        <div class="input-group">
                            <button type="button" class="botonF2 btn-danger" style="background-color: red" data-dismiss="modal">
                                <i class="fas fa-times-circle"></i>
                            </button>
                        </div>
                    </td>
                    <td align="left" class="col-xs-1">
                        <div class="input-group">
                            <button type="submit" class="botonF1Modal btn-success" style="background-color: green">
                                <i class="fas fa-check-circle"></i>
                            </button>
                        </div>
                    </td>
                </tr>
                <tr>
                    <td colspan="4" align="center" style="text-align: center">
                        @*@ViewBag.Tienda*@
                        @Html.DisplayNameFor(model => model.FirstOrDefault().ArticuloKFC.Descripcion)
                    </td>
                </tr>

            </table>
        </div>

        <div class="panel-body">
            <div id="unId40" class="table-responsive">
                <table class="table table-striped table-hover" style="width:100%" id="dataTable" cellspacing="0">

                    @*<thead>
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.FirstOrDefault().Tienda.Restaurante)
                            </th>
                            @foreach (var item in Model)
                            {
                                <th>
                                    @Html.DisplayFor(modelItem => item.ArticuloKFC.Descripcion, new { htmlAttributes = new { @class = "form-control responsivoUsuarioText inputModal", @required = "true", @oninvalid = "setCustomValidity('Este campo es obligatorio')", @oninput = "setCustomValidity('')", @style = "text-transform:uppercase", placeholder = "Artículo" } })
                                </th>
                            }
                        </tr>
                    </thead>*@

                    <tbody>
                        @foreach (var item2 in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item2.ArticuloKFC.Descripcion, new { htmlAttributes = new { @class = "form-control responsivoUsuarioText inputModal", @style = "text-transform:uppercase" } })
                                </td>
                                <td>
                                    <input class="form-check checkbox" type="checkbox" value="@item2.Seleccionado" name="seleccionado" id="seleccionado" checked="@item2.Seleccionado" @item2.Seleccionado />
                                    @*@Html.CheckBoxFor(modelItem => item2.Seleccionado, htmlAttributes: new { @class = "form-control form-check checkbox" })*@
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>

                @*<div class="form-group">
                        @foreach (var item in Model)
                        {
                            <div class="col-sm-12">
                                @Html.EditorFor(modelItem => item.Tienda.Restaurante, new { htmlAttributes = new { @class = "form-control inputModal", @disabled = "disabled", @style = "text-transform:uppercase", placeholder = "Material" } })
                            </div>
                        }
                    </div>
                    <div class="form-group">
                        @foreach (var item in Model)
                        {
                            <div class="col-sm-10">
                                @Html.EditorFor(modelItem => item.ArticuloKFC.Descripcion, new { htmlAttributes = new { @class = "form-control inputModal", @disabled = "disabled", @style = "text-transform:uppercase", placeholder = "Material" } })
                            </div>
                            <div class="col-sm-2" style="text-align: center">
                                @Html.CheckBoxFor(modelItem => item.Seleccionado, htmlAttributes: new { @class = "form-check" })
                            </div>
                        }
                    </div>*@
            </div>
        </div>
}
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
